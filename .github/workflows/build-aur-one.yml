jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Setup Rclone action
      uses: AnimMouse/setup-rclone@v1
      with:
        rclone_config: ${{ secrets.RCLONE_CONFIG }}
    - name: Get package name
      run: |
       PKGNAME=`curl "https://raw.githubusercontent.com/YidaozhanYa/arch-build/main/pkgname.txt"`
       echo "PKGNAME=${PKGNAME}" >> $GITHUB_ENV
    - uses: Brx86/build-aur-action@yay
      with:
        pkgname: ${{ env.PKGNAME }}
    - name: Upload package
      env:
        PKGNAME: ${{ env.PKGNAME }}
      run: |
       rclone copy ./${PKGNAME}/*.zst e5:/pkgbuild/x86_64/
    - uses: YidaozhanYa/update-db-for-repo@master
      with:
        tag: x86_64
    - name: Upload generated database
      run: |
       rclone copy ./yidaozhan.db e5:/pkgbuild/x86_64/yidaozhan.db
       rclone copy ./yidaozhan.db.tar.gz e5:/pkgbuild/x86_64/yidaozhan.db.tar.gz
       rclone copy ./yidaozhan.db.tar.gz.old e5:/pkgbuild/x86_64/yidaozhan.db.tar.gz.old
       rclone copy ./yidaozhan.files e5:/pkgbuild/x86_64/yidaozhan.files
       rclone copy ./yidaozhan.files.tar.gz e5:/pkgbuild/x86_64/yidaozhan.files.tar.gz
       rclone copy ./yidaozhan.files.tar.gz.old e5:/pkgbuild/x86_64/yidaozhan.files.tar.gz.old
name: build-aur-one
on:
  push:
    paths:
      - 'pkgname.txt'
  workflow_dispatch: null
